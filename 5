//  1. მოცემულია 2 ცვლადი, გავუცვალოთ ადგილები. (არ გამოვიყენოთ დამატებითი ცვლადი)

{
    int a;
    int b;
    
    a = 3;
    b = 4;
    
    a = a + b;
    b = a - b;
    a = a - b;
}

//  2. ვიპოვოთ მართკუთხედის ფართობი. მოცემულია 2 მთელი დადებითი რიცხვი a და b. აღვწეროთ ცვლადი სახელად area და მივანიჭოთ მართკუთხედის ფართობი.

{
    int a;
    int b;
    int area;

    a = 2;
    b = 3;
    area = a * b;
}

//  3. პირობა იგივეა რაც მეორე ამოცანაში, ოღონდ ვიპოვოთ პერიმეტრი.

{
    int a;
    int b;
    int area;
    
    a = 2;
    b = 3;
    
    area = (a + b) * 2;
}

/*  4. ფუნთუშა ღირს a ლარი და b თეთრი. გამოთვალეთ რამდენი თეთრი არის გადასახდელი n რაოდენობის ფუნთუშაში.
    პროგრამაში მოცემულია სამი მთელი რიცხვი: a, b, n შესაბამისადდავთვალოთ რამდენი თეთრი უნდა გადავიხადოთ და მოვათავსოთ result ცვლადში.
*/

{
    int a;
    int b;
    int n;
    int result;
    
    a = 2;
    b = 30;
    n = 5;
    result = a * 100 * n + b * n;
}
